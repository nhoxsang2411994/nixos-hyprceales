{ pkgs, inputs, ... }: {
# waybar declarative configs
  programs.wofi = {
    enable = true;
  };

  home.packages = with pkgs; [
    inputs.waybar-git.packages.x86_64-linux.default
    googlesans-code
  ];
}
